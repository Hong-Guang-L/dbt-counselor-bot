# 开发日志 - DBT情感咨询聊天机器人

> **编写：Emma** | **审核：Mike** | **团队：米醋电子工作室**
>
> **项目目标**：训练一个具有DBT情感咨询能力的聊天机器人，部署在Orange Pi 5上

---

## 2026-02-14

### 任务：训练环境搭建

### 完成的工作

1. **环境检测**
   - 检测到系统Python 3.9.2
   - 检测到PyCharm虚拟环境 `.venv`（Python 3.14）
   - 发现已安装的库：torch 2.5.1+cu121, torchvision, torchaudio

2. **依赖库整理**
   - 整理出需要安装的核心库清单
   - 用户在PyCharm中完成了这些库的安装

3. **训练框架安装**
   - 确认用户安装了LLaMA-Factory训练框架
   - 确认用户安装了Unsloth加速库

### 安装的依赖包

| 包名 | 说明 |
|------|------|
| torch | PyTorch深度学习框架 |
| transformers | Hugging Face transformers库 |
| sentencepiece | 文本分词工具 |
| gradio | Web界面框架 |
| llamafactory | LLaMA-Factory训练框架 |
| unsloth | 训练加速库 |

---

## 2026-02-15

### 任务：模型选型与数据集调研

### 完成的工作

1. **部署环境分析**
   - Orange Pi 5 内存：8GB
   - 需同时运行：LLM + STT（语音转文字）+ TTS（文字转语音）

2. **模型选型对比**

   | 模型 | 参数 | 量化后大小 | 中文能力 | 推荐度 |
   |------|------|------------|----------|--------|
   | **Qwen3-1.7B** | 1.7B | ~1.2GB | ⭐⭐⭐⭐⭐ | ⭐首选 |
   | Qwen2.5-1.5B | 1.5B | ~1GB | ⭐⭐⭐⭐⭐ | ⭐⭐ |
   | Gemma-2-2B | 2B | ~1.5GB | ⭐⭐⭐ | ⭐ |

3. **最终选择：Qwen3-1.7B**

4. **数据集调研**
   - ESConv - 情感支持对话数据集
   - PsyQA - 中文心理健康问答数据集

---

## 2026-02-16

### 任务：数据集生成、清洗与训练启动

### 完成的工作

#### 1. 数据集生成

**生成CBT数据集（500条）**：
- 针对空巢老人情感需求
- 包含认知重构、行为激活等技术
- 格式：ShareGPT对话格式

**生成DBT数据集（500条）**：
- 四大技能模块：正念、情绪调节、人际效能、痛苦耐受
- 针对空巢老人场景
- 格式：ShareGPT对话格式

#### 2. 数据集清洗与合并

**处理PsyDTCorpus数据集**：
- 原始数据：心理咨询对话
- 清洗策略：提取有效对话，过滤无效内容
- 最终保留：高质量心理咨询对话

**处理catch_mdpcot数据集**：
- 问题：包含大量"hoden"思维过程标记
- 解决方案：开发专用清洗函数提取真实回复
- 清洗函数核心逻辑：
  ```python
  def extract_real_response(output):
      if "hoden" in output:
          parts = output.split("hoden", 1)
          # 提取思维过程后的真实回复
          ...
  ```

**最终合并数据集**：
- 总样本数：**6,734条**
- 来源：CBT数据 + DBT数据 + 清洗后的PsyDTCorpus + 清洗后的catch_mdpcot
- 格式：ShareGPT格式（conversations字段）

#### 3. 训练环境配置

**遇到的问题**：

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| Python 3.14兼容性 | dill库不兼容 | 创建Python 3.10环境 |
| PyTorch CPU版本 | 无法使用GPU | 安装CUDA 11.8版本 |
| 模板不存在 | qwen3_nothink模板未支持 | 改用qwen模板 |

**环境配置步骤**：

1. 创建Python 3.10虚拟环境
   ```powershell
   py -3.10 -m venv .venv310
   ```

2. 安装PyTorch CUDA版本
   ```powershell
   # 下载torch-2.5.1+cu118-cp310-cp310-win_amd64.whl
   py -3.10 -m pip install torch-2.5.1+cu118-cp310-cp310-win_amd64.whl
   ```

3. 验证CUDA
   ```
   PyTorch版本: 2.5.1+cu118
   CUDA可用: True
   GPU设备: NVIDIA GeForce RTX 3060 Laptop GPU
   ```

#### 4. 训练配置

**train.yaml配置**：
```yaml
model_name_or_path: models/Qwen/Qwen3-1___7B
stage: sft
finetuning_type: lora
lora_target: all

dataset: dbt_counseling
dataset_dir: datasets
template: qwen
cutoff_len: 1024

per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 5.0e-5
num_train_epochs: 3.0
fp16: true

lora_rank: 8
lora_alpha: 16
lora_dropout: 0.1
```

#### 5. 训练启动

**训练状态**：
- 训练框架：LLaMA-Factory
- 训练方法：LoRA微调
- 数据集：6,734条
- 总步数：2,526步
- 当前进度：7%（179/2526步）
- 预计时间：约6小时

**Loss下降趋势**：
| 步数 | Loss |
|------|------|
| 10 | 3.77 |
| 50 | 3.23 |
| 100 | 2.68 |
| 179 | 2.27 |

训练正常进行中 ✅

### 生成的文件

| 文件 | 说明 |
|------|------|
| `datasets/train_final.json` | 最终训练数据集（6,734条） |
| `datasets/dataset_info.json` | LLaMA-Factory数据集配置 |
| `train.yaml` | 训练配置文件 |
| `check_cuda.py` | CUDA验证脚本 |

---

## 2026-02-17

### 任务：训练完成与模型部署

### 完成的工作

#### 1. 训练完成

**训练结果**：

| 指标 | 值 |
|------|-----|
| 训练轮数 | 3.0 ✅ |
| 最终Loss | 2.0131 |
| 训练时长 | 5小时57分16秒 |
| 总步数 | 2,526步 |
| 模型保存位置 | `output/qwen3_dbt_lora` |

**Loss变化趋势**：

| 阶段 | Loss |
|------|------|
| 开始 | 3.77 |
| 中期 | 2.68 |
| 结束 | 2.01 |

Loss从3.77降到2.01，训练效果良好 ✅

#### 2. LoRA权重合并

**合并配置 `merge.yaml`**：

```yaml
model_name_or_path: models/Qwen/Qwen3-1___7B
adapter_name_or_path: output/qwen3_dbt_lora
template: qwen
finetuning_type: lora
export_dir: output/qwen3_dbt_merged
export_size: 2
export_device: cpu
export_legacy_format: false
```

**执行合并**：

```powershell
py -3.10 -m llamafactory.cli export merge.yaml
```

**合并结果**：

| 项目 | 状态 |
|------|------|
| 合并源 | `output/qwen3_dbt_lora` ✅ |
| 合并目标 | `output/qwen3_dbt_merged` ✅ |
| 模型大小 | ~3.4GB（分2个文件） |

#### 3. GGUF格式转换

**安装llama.cpp依赖**：

```powershell
pip install -r llama.cpp-master/requirements.txt
```

**转换为GGUF**：

```powershell
py -3.10 llama.cpp-master/convert_hf_to_gguf.py output/qwen3_dbt_merged --outfile output/dbt-counselor-f16.gguf --outtype f16
```

**转换结果**：

| 文件 | 大小 |
|------|------|
| `dbt-counselor-f16.gguf` | 3.4GB ✅ |

#### 4. Ollama部署

**创建Modelfile**：

```
FROM ./dbt-counselor-f16.gguf

TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{- end }}
<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
"""

PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER top_k 40

SYSTEM """你是一位专业的DBT心理咨询师..."""
```

**导入Ollama**：

```powershell
ollama create dbt-counselor -f output/Modelfile
```

#### 5. 模型测试

**测试结果**：

| 测试问题 | 回复质量 |
|----------|----------|
| 孤独感咨询 | ✅ 正常 |
| 情感支持 | ✅ 正常 |
| 危机干预 | ⚠️ 需优化 |

---

## 2026-02-18

### 任务：尝试训练Qwen3-4B模型与模型量化

### 完成的工作

#### 1. 下载Qwen3-4B模型

**下载脚本 `download_qwen3_4b.py`**：

```python
from modelscope import snapshot_download

model_dir = snapshot_download(
    'Qwen/Qwen3-4B',
    cache_dir='./models'
)
```

**下载结果**：

| 项目 | 值 |
|------|-----|
| 模型路径 | `models/Qwen/Qwen3-4B` |
| 模型大小 | ~8GB |

#### 2. 尝试训练4B模型（失败）

**尝试方案**：

| 方案 | 说明 | 结果 |
|------|------|------|
| FP16训练 | 直接训练 | ❌ 显存不足 |
| QLoRA 4-bit | 量化训练 | ❌ bitsandbytes不支持Windows |
| DeepSpeed ZeRO-2 | CPU Offload | ❌ DeepSpeed不支持Windows |
| 低显存配置 | Gradient Checkpointing | ❌ 仍然不足 |

**失败原因分析**：

| 问题 | 说明 |
|------|------|
| RTX 3060显存 | 只有6GB，4B模型需要~10GB |
| bitsandbytes | Windows不支持GPU版本 |
| DeepSpeed | Windows兼容性差 |

**结论**：RTX 3060 (6GB) 无法训练Qwen3-4B，只能训练1.7B模型。

#### 3. C++环境配置

**安装Visual Studio Build Tools**：

- 版本：Visual Studio 2022 Build Tools
- 组件：使用C++的桌面开发
- 大小：~2.5GB

**安装CMake**：

- 版本：CMake 4.0.0
- 配置：添加到系统PATH

**验证安装**：

```powershell
where cl
# 输出：C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.42.34433\bin\Hostx86\x86\cl.exe

where cmake
# 输出：C:\Program Files\CMake\bin\cmake.exe
```

#### 4. 编译llama.cpp

**编译步骤**：

```powershell
cd llama.cpp-master
mkdir build
cd build
cmake ..
cmake --build . --config Release
```

**编译结果**：

| 工具 | 路径 | 状态 |
|------|------|------|
| llama-quantize.exe | `build\bin\Debug\llama-quantize.exe` | ✅ |
| llama-cli.exe | `build\bin\Debug\llama-cli.exe` | ✅ |
| llama-server.exe | `build\bin\Debug\llama-server.exe` | ✅ |

#### 5. 模型量化

**Qwen3-1.7B量化**：

```powershell
# 转换为GGUF
python convert_hf_to_gguf.py output/qwen3_dbt_merged --outfile output/dbt-counselor-f16.gguf --outtype f16

# 量化为Q4_K_M
llama-quantize.exe output/dbt-counselor-f16.gguf output/dbt-counselor-q4_k_m.gguf Q4_K_M
```

**Qwen3-4B量化**：

```powershell
# 转换为GGUF
python convert_hf_to_gguf.py models/Qwen/Qwen3-4B --outfile output/qwen3-4b-f16.gguf --outtype f16

# 量化为Q4_K_M
llama-quantize.exe output/qwen3-4b-f16.gguf output/qwen3-4b-q4_k_m.gguf Q4_K_M
```

**量化结果对比**：

| 模型 | FP16大小 | Q4_K_M大小 | 压缩比 |
|------|----------|------------|--------|
| Qwen3-1.7B | 3.4GB | 1.2GB | 65% |
| Qwen3-4B | 7.5GB | 2.3GB | 69% |

#### 6. 模型测试对比

**测试问题：孤独感咨询**

| 模型 | 回复长度 | 专业性 | 共情能力 |
|------|----------|--------|----------|
| Qwen3-1.7B (微调) | 短 | 一般 | 一般 |
| **Qwen3-4B (未微调)** | **长且详细** | **强** | **强** |

**测试问题：自杀意念**

| 模型 | 热线触发 | 回复质量 |
|------|----------|----------|
| Qwen3-1.7B (微调) | ❌ 未触发 | 简单 |
| Qwen3-4B (未微调) | ⚠️ 部分触发 | 详细专业 |

**结论**：Qwen3-4B虽然未经微调，但回复质量明显优于微调后的1.7B模型。

#### 7. 最终模型选择

| 模型 | 大小 | DBT微调 | 回复质量 | 推荐场景 |
|------|------|----------|----------|----------|
| dbt-counselor (1.7B) | 1.2GB | ✅ 有 | 一般 | 显存受限场景 |
| **qwen3-4b-counselor** | **2.3GB** | ❌ 无 | **更好** | **Orange Pi 5 (8GB)** |

### 生成的文件

| 文件 | 说明 |
|------|------|
| `train_4b.yaml` | 4B模型训练配置 |
| `train_4b_qlora.yaml` | QLoRA训练配置 |
| `train_4b_deepspeed.yaml` | DeepSpeed训练配置 |
| `train_4b_lowmem.yaml` | 低显存训练配置 |
| `ds_config.json` | DeepSpeed配置 |
| `output/qwen3-4b-f16.gguf` | 4B模型FP16格式 |
| `output/qwen3-4b-q4_k_m.gguf` | 4B模型量化版 |
| `output/dbt-counselor-q4_k_m.gguf` | 1.7B模型量化版 |
| `output/Modelfile-4b` | 4B模型Ollama配置 |

### 下一步计划

1. ✅ 部署到Orange Pi 5
2. ✅ 添加语音功能（STT/TTS）
3. ⏳ 优化危机干预能力
4. ⏳ 研究昇腾NPU加速方案
```

**部署成功** ✅

### 生成的文件

| 文件 | 说明 |
|------|------|
| `output/qwen3_dbt_lora/` | LoRA权重目录 |
| `output/qwen3_dbt_merged/` | 合并后模型目录 |
| `output/dbt-counselor-f16.gguf` | GGUF格式模型 |
| `output/Modelfile` | Ollama配置文件 |
| `merge.yaml` | 模型合并配置 |

### 下一步计划

1. ✅ 训练完成
2. ✅ LoRA权重合并
3. ✅ GGUF格式转换
4. ✅ Ollama部署测试
5. ⏳ 部署到Orange Pi 5
6. ⏳ 添加语音功能（STT/TTS）

---

## 项目进度总览

| 阶段 | 状态 | 完成度 |
|------|------|--------|
| 环境搭建 | ✅ 完成 | 100% |
| 模型选型 | ✅ 完成 | 100% |
| 数据集准备 | ✅ 完成 | 100% |
| 模型训练 | 🔄 进行中 | 7% |
| 模型导出 | ⏳ 待开始 | 0% |
| 模型部署 | ⏳ 待开始 | 0% |

---

*本文档由米醋电子工作室维护，版权所有*
